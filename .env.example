DATABASE_URL=postgresql://user:password@localhost:5432/findmas
JWT_SECRET=cok_gizli_sifre_buraya_yaz
NODE_ENV=development
PORT=3000

# Firebase (FCM bildirişlər üçün)
# Firebase Console → Project Settings → Service accounts → Generate new private key
# JSON faylının içərisini bir sətirə çevir
FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
